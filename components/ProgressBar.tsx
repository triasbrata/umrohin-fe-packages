'use client'

import { Next13ProgressBar } from 'next13-progressbar'

export const ProgressBar = () => {
  return <Next13ProgressBar height="3px" color="#EE204D" options={{ showSpinner: false }} showOnShallow />
}
